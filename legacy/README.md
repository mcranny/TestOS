 Legacy 32-bit Multiboot TestOS (i386)
#
# Archived. The active OS is the x86-64 UEFI tree at the repository root.
# Build from this directory if you need the old kernel:
#   cd legacy && make && make run

